overwrite: true
schema: 'http://localhost:4001'
generates:
  # GENERATES SERVER TYPES
  libs/server/data-access-models/src/lib/generated/graphql-codegen-typings.d.ts:
    plugins:
      - typescript
      - typescript-mongodb
    config:
      enumsAsTypes: true
    schema:
      - apps/server/graphql-api/src/**/*.graphql
