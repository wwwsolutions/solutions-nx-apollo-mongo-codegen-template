overwrite: true
schema: 'http://localhost:4001'
generates:
  apps/server/graphql-api/src/graphql-codegen-typings.d.ts:
    plugins:
      - typescript
      - typescript-mongodb
    config:
      enumsAsTypes: true
    schema:
      - apps/server/graphql-api/src/**/*.graphql
